/* The below function controls the header bar. */
import "./header.css";

function Header() {
  // The "xxxxxxxx" is currently just some filler text.
  return <h1 className="header">xxxxxxxx</h1>;
}

export default Header;
